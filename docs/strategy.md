# 戦略 — Figmaナビ

## ジャンル実験（日次 PDCA の上位層）— 毎朝ルーチンは最初にこの節を読む

> **正本**: sns-hub [`docs/strategy/genre-experiment.md`](https://github.com/nannantown/sns-hub/blob/main/docs/strategy/genre-experiment.md)（2026-09-14 オーナー決定「AI系 IG 以外の 5 アカウントはジャンル/型を変えながら試す。コーヒーは販促維持」）。この節はこのリポの 2 アカウント分の**写し**で、食い違ったら正本が勝つ。
> 下の「Meta PDCA」は「このジャンルの中でどのネタ・method が効くか」を毎日最適化する層。この節は「**このジャンル/型そのものを続けるか切るか**」を 14 日ごとに判定する上位層で、**Meta PDCA より優先する**。
> この節の書き換えは人間だけ（ルーチンは提案まで）。IG と YT は**別アカウント**として別々に評価し、数字を合算しない。

### このリポの試行台帳（写し）

| アカウント | 試行 # | ジャンル / 型 | 開始日 S | 型の初回投稿日 F | 導入時（2026-09-14）の判定 |
|---|---|---|---|---|---|
| IG Figmaナビ（既存デザインアカウント。ハンドル未記録） | #0 | 平日: デザインニュース / 週末: Tips・Before/After・UI 批評 / タイトル固定「【デザインニュース】…｜日付 #Shorts」 | 2026-09-14 | 2026-04-22 | 配信死亡（09-01..09-14: views 中央値 5 / 保存合計 0, n=12） |
| YT HAL- AI情報カフェ (@hal-ai-9000) | #0 | 同上 | 2026-09-14 | 2026-04-22 | 配信死亡（09-01..09-14: views 中央値 1, n=12） |

- 試行 #0 = 2026-04-22 の運用開始からの現行型。S はルール導入日、F は現行型を始めた日
- 新しい型を始める PR では、sns-hub の台帳とこの表を**同じ日に**書き換える（試行 # を進め、S = F = 新しい型の初回投稿日）
- 試行 #1 の候補（2026-09-14 時点で別カードが実装中）: AI ツール TOP5（Product Hunt）
- 09-13・09-14 は `daily-video.yml` が失敗しており `data/performance-history.json` に未記録（投稿・取得の失敗が続くと n が減る。n < 7 なら判定保留）

### 判定窓・経過日・次の判定日

```
d = 今日 − S（日数。日付はすべて JST）
d ≥ 14 かつ d が 14 の倍数 → 今日は判定日。判定窓 = (今日 − 14)..(今日 − 1)、次の判定日 = 今日 + 14
それ以外                  → 経過日 = Day (d mod 14 + 1) / 14、判定窓 = max(F, 今日 − 13)..今日、
                             次の判定日 = S + 14 × (floor(d / 14) + 1)
```

例: 09-15 は Day 2 / 14・判定窓 09-02..09-15・次の判定日 09-28。09-28 は判定日（判定窓 09-14..09-27）。

### 判定指標と集計コマンド

- **IG**: 判定窓の `instagram.views` の**中央値** + `instagram.saved` の**合計**（`instagram` か `instagram.views` が null の回は除外）
- **YT**: 判定窓の `stats.views` の**中央値**（`stats.updatedAt` が null の回は除外）
- n は IG / YT で別々に数える。**n < 7 なら判定保留**。IG と YT を足したり平均したりしない

```bash
# FROM / TO = 判定窓（上の計算式）
FROM=2026-09-02; TO=2026-09-15
jq -r --arg from "$FROM" --arg to "$TO" '
  def median: sort | if length==0 then null elif length%2==1 then .[(length/2|floor)] else ((.[length/2-1]+.[length/2])/2) end;
  [.videos[] | select(.date >= $from and .date <= $to)] as $v
  | ($v | map(select(.stats.updatedAt != null) | (.stats.views // 0))) as $yt
  | ($v | map(select(.instagram != null and .instagram.views != null) | .instagram)) as $ig
  | "window=\($from)..\($to) posts=\($v|length)",
    "YT views median=\($yt|median) (n=\($yt|length))",
    "IG views median=\($ig|map(.views)|median) saved_sum=\($ig|map(.saved // 0)|add // 0) (n=\($ig|length))"
' data/performance-history.json
```

### 閾値（表の上から順に当てはめ、最初に該当した行が判定。初期値・仮置き）

| 判定 | IG | YT |
|---|---|---|
| **配信死亡** | views 中央値 < 10 **かつ** 保存合計 < 5 | views 中央値 < 5 |
| **切替候補** | views 中央値 < 50 **かつ** 保存合計 < 5 | views 中央値 < 20 |
| **続行** | 上記以外 | 上記以外 |

判定は指標だけで機械的に出す（「もう少し様子を見たい」は理由にしない）。閾値はルーチンが変えない（おかしいと思ったら「戦略更新提案」に書く）。

### モードの決め方（毎朝、アカウントごと）

モードは `通常` / `切替候補` / `配信死亡モード` の 3 つ。

1. **前回モード** = `docs/pdca/` の最新レポートの「ジャンル試行の状態」のモード列。節が無ければ上の表の「導入時の判定」（配信死亡 → `配信死亡モード（2026-09-14〜）`）。試行 # が前回と違えば引き継がず `通常` から
2. **今日の判定値** = 判定窓の指標を閾値に当てはめた結果（n < 7 なら判定保留）
3. **決定**（上から順に）:
   - 判定保留 → 前回モードのまま
   - 前回が `配信死亡モード` → **判定日かつ判定値が配信死亡以外**のときだけ出る（続行 → `通常`、切替候補 → `切替候補`）。それ以外は `配信死亡モード` のまま
   - 判定値が配信死亡 → `配信死亡モード` に入る（判定日でなくても。宣言日 = 今日）
   - 今日が判定日 → 判定値どおり（続行 → `通常`、切替候補 → `切替候補`）
   - それ以外 → 前回モードのまま

### 配信死亡モード中の日次 PDCA

1. **method 切り口の最適化を止める**: 配信死亡モードのアカウントの数字で method の TOP/WORST 比較・Method 提案・80/20 選定・WORST method の凍結をしない（0 と 1 を比べても何も分からない）
   - **2 アカウントとも配信死亡モード** → method は**性能データで選ばない**。コンテンツ柱比率・曜日フォーマット・連続回避・NG パターンの制約だけで選ぶ（method 別テーブルは参考表示のみ）
   - **片方だけ配信死亡モード** → method の比較と 80/20 は**生きている側のアカウントの指標だけ**で行う
2. **提案は構造実験だけ**: タイトル個別化 / 型変更 / ジャンル変更。各提案に「何を変えるか / 何で測るか / 14 日後の合格ライン」を書く
3. **このリポの制約**: ジャンル・型・タイトル方針のすべてを変えてよい（販促対象の Figmaナビへの導線は下の「ファネル」に従う）
4. **投稿は止めない**。ネタ調査・原稿生成は通常どおり。型の切替は人間が決めて PR で入れる（ルーチンは提案まで）

### 日次レポート（`docs/pdca/$TODAY.md`）の冒頭に書く節

タイトル直下に次の節を書く。判定日なら「ジャンル判定」、配信死亡モードのアカウントがある日は「構造実験の提案」も書く。

```markdown
## ジャンル試行の状態

| アカウント | 試行 # | 開始日 | 経過日 | 判定窓 (n) | 判定指標の現在値 | モード | 次の判定日 |
|---|---|---|---|---|---|---|---|
| IG Figmaナビ | #0 | 2026-09-14 | Day N / 14（判定日なら「判定日」） | MM-DD..MM-DD (n=…) | views 中央値 … / 保存合計 … | 通常 / 切替候補 / 配信死亡モード（YYYY-MM-DD〜） | YYYY-MM-DD |
| YT HAL- AI情報カフェ | #0 | 2026-09-14 | … | MM-DD..MM-DD (n=…) | views 中央値 … | … | YYYY-MM-DD |

- 今日の判定: なし（判定日ではない） / 判定日 →「ジャンル判定」節 / 配信死亡モードの新規宣言・解除
- 今日の method 方針: 通常（Meta PDCA・80/20） / 性能データで選ばない（2 アカウントとも配信死亡モード） / <生きている側のアカウント> の指標だけで選ぶ

## ジャンル判定（判定日のみ）
- <アカウント>: 続行 / 切替候補 / 配信死亡 — 根拠（指標値と閾値） — 次ジャンル候補 2〜3 案（切替候補・配信死亡のとき）

## 構造実験の提案（配信死亡モードのアカウントがある日のみ）
- <アカウント>: <タイトル個別化 / 型変更 / ジャンル変更> — 何を変えるか / 何で測るか / 14 日後の合格ライン
```

## 売りたいもの

**Figma プラグイン「Figmaナビ」の有料サブスクリプション**。
AI がデザインを生成する。デザインシステム準拠・チームのルール入力対応が強み。
価格は **¥980 / 約50命令** など、初心者も買える低価格帯で構成（無料枠あり・3プラン）。

## 運用フォーマット: ニュース中心

**「毎朝チェックしないと乗り遅れる」FOMO を作る**ために、コンテンツはニュース性を主軸とする。Tips のみだと習慣化されず、既存200人を超えて伸ばすのが難しい。

| 曜日 | コンテンツ型 | 優先度 |
|---|---|---|
| 月〜金 | **毎朝のデザインニュース**（Figma/プラグイン/業界/AI×デザイン） | Primary |
| 土・日 | **Tips / Before-After / ポートフォリオ事例** | Secondary |

週末の Tips はニュースで触れた話題の実践編（「今週発表された Figma の新機能、早速試した」等）として接続すると、平日ニュース→週末深掘りの循環が生まれる。

## ペルソナ（優先順）

### Primary: デザイナー志望者・駆け出しデザイナー
- 既存 YouTube デザイン講座チャンネルに **約200人のサブスクライバー** がいる
- その層の延長として、スキルアップ中・転職準備中・独学者が中心
- Figma は触れるが、ゼロからの作例作りに時間がかかる
- ポートフォリオ / 実案件想定の作例 / LP作成 などの実用シーンを抱えている
- ¥980 なら学習投資として手が出る
- **既存の信頼関係があるため、day-1 の転換源として最重要**

### Secondary: 個人デザイナー / フリーランス
- 量をこなしたい。下書きの時短が直接売上に効く
- Layermate 等と比較検討する層
- 価格で勝てる余地がある

### Tertiary: 非デザイナー職（PM / エンジニア / 創業者）
- 「デザイナー雇えない」「外注の時間も予算もない」
- LP、資料、管理画面、プロトタイプを自分で作る必要がある
- 業務時間短縮＝対価を払う動機が強い

## 差別化ポイント

1. **価格** — ¥980/約50命令の低価格帯。Layermate 等よりわかりやすく安い
2. **デザインシステム / ルール入力可能** — 将来の法人展開の布石
3. **チーム利用を前提とした設計**
4. **日本語ネイティブ対応**（UI / サポート / 事例動画）
5. **教える人が作っている** — デザイン講座運営者による、学習者の詰まりどころを理解したプラグイン

## コンテンツ柱（ニュース中心ver）

| 柱 | 比率 | 型 | ねらい |
|---|---|---|---|
| ①**Figma 公式ニュース** | 25% | News | 新機能、リリース、Config 発表、価格改定 |
| ②**プラグイン / エコシステム** | 20% | News | Community の注目新作、AI 系ツールの新着、Figmaナビも自然にここで紹介 |
| ③**業界・デザインニュース** | 20% | News | 大手リブランド、買収、デザイン賞、トレンド、AI×デザイン |
| ④**Tips / Figma 実践**（講座の延長） | 15% | Evergreen | 既存200人の期待に応える、学習者向け |
| ⑤**Before/After / ポートフォリオ事例** | 10% | Evergreen | Figmaナビの実用価値を定量訴求 |
| ⑥**UI 批評（学習要素）** | 10% | Evergreen | 議論・保存を喚起、学習者に刺さる |

### 具体ネタ例
- ①: 「Figma が新機能 Grid を発表、使い方 30 秒解説」「Config 2026 発表、要点まとめ」
- ②: 「Figma Community の新着、AI 生成系プラグイン3選」「〇〇プラグイン、Figmaナビとどう違う」
- ③: 「Apple が新しいデザイン言語を発表」「大手 SaaS のリデザイン、何が変わった」
- ④: 「Auto Layout 実務で速くなる使い方」「変数機能、独学で気づきにくい3つ」
- ⑤: 「LPのFVをAIで5秒生成 vs 手で30分」「ポートフォリオ10本、AIで1日で作った記録」
- ⑥: 「このアプリ、ボタンが見つからない理由」「このフォームで離脱する3つの原因」

## 探索方針（Discovery Strategy）

### 英語優先原則

**英語ソースを先に見る**。デザインツール系・業界動向系のニュースは、英語が日本語より 1〜3 日早い。英語で拾って日本語で噛み砕く方が速報性・独自性で勝る。
日本語ソースは補完（日本企業のリデザイン、日本語コミュニティ声など）。

### 推奨ソース（シード）

**英語（優先）**:
- Figma changelog / Figma Blog — 公式リリース情報
- Figma Community — 新着プラグイン
- UX Collective (Medium) — 業界論考
- Smashing Magazine — UX/UI 実践
- Designer News / Dribbble Trending
- Hacker News "design" keyword — 技術者層の議論
- Product Hunt — 新プロダクト、AI ツール
- Awwwards — 受賞作品
- X（英語）の著名デザイナー（Khoi Vinh, Mike Buzzard 等）
- r/Design, r/UI_Design, r/web_design

**日本語（バックアップ）**:
- cocoda（デザイナーコミュニティ）
- note デザイン記事
- X 日本語デザイナー界隈
- Designer News 日本語記事

### Discovery Methods（カテゴリタグ）

| タグ | 説明 |
|---|---|
| `news-en` | 英語のデザイン/テック業界メディア |
| `news-ja` | 日本語メディア（cocoda, note等） |
| `official-src` | Figma changelog / 公式ブログ / リリースノート |
| `figma-community` | Figma Community の新着プラグイン |
| `social-en` | X / Reddit / HN の英語デザイナー界隈 |
| `social-ja` | X 日本語デザイナー界隈 |
| `product-launches` | Product Hunt / Awwwards / Dribbble Trending |
| `expert-blog` | 個人デザイナー・業界有識者の発信 |

### 80/20 Exploit/Explore

- 過去14日で視聴率上位3位の method は 80% の頻度で使う（exploit）
- 残り 20% は**新しい method を試す**（explore）
- Explore でヒットした method は次サイクルで exploit 側に昇格

### Meta PDCA（探し方の PDCA）

> 冒頭の「ジャンル実験」節が優先する。**配信死亡モードのアカウントの数字では、以下の method 比較・80/20・凍結をしない**（2 アカウントとも配信死亡モードなら性能データで method を選ばない。片方だけなら生きている側の指標だけで比較する）。

毎日のルーチンで以下を分析:

1. 直近 14 日の `performance-history.json` から **method → 平均 views / プラグインインストール** テーブルを作成
   - YouTube は `stats.views`、Instagram Reels は `instagram.views`（加えて `reach` / `shares` / `saved`）を**別列で並べる**。両者を合算しない（計測定義が違う）
   - `instagram` が `null` の動画は IG 投稿と突き合わせ不能だった回。IG 側の平均から除外する
   - IG insights は最大 48 時間遅れる。前日・当日分の IG 値は暫定として扱う
2. TOP 3 / WORST 3 method を特定
3. 「今日はどの method で探すか」を 80/20 ルールで決める
4. 新しい method アイデアは PDCA レポートの「Method 提案」セクションに記録
5. **WORST method は 2 週連続なら一旦凍結**

## NG パターン

| NG | 理由 |
|---|---|
| プロ向けの高度な議論 | Primary 層が置いていかれる |
| 「AIがデザイナーの仕事を奪う」系 | 学習中の視聴者を不安にさせる。**学習者の味方として振る舞う**のが方針 |
| プラグインの機能説明メイン | 興味ない層が離脱。必ず「学習者の課題」から入る |
| 英語/日本語の中途半端な混在 | JPチャンネルはJP主。ただし**入力情報は英語**、出力が日本語 |
| 「AIに任せれば考えなくていい」系訴求 | 学習者向けに不誠実。AIは**補助輪**として位置づける |
| **1週間以上前のニュースを新着扱い** | 鮮度が命。`freshness_hours > 168` は原則 NG |
| **出典不明ニュース** | 信用毀損。ソース URL を必ず記録・明示する |

## 英語展開方針

- YouTube: 英語字幕必須、動画内テキストは日英併記 or 英語版を別チャンネルで運用
- Instagram: キャプションの冒頭は英語、2行目以降日本語
- 判断: まず JP で 500 インストール到達したら EN 展開を本格化

## ファネル

```
[既存 YouTube デザイン講座フォロワー 200]
  ↓ 新動画（毎朝デザインニュース）
SNS 視聴（ニュース + 週末Tips）
  ↓ プロフィールへ
プロフィール bio（「AIでデザインを作るFigmaプラグイン / ¥980〜」）
  ↓ Figma Community URL
Figma Community ページ（プラグインインストール）
  ↓ インストール
無料プランで試す
  ↓ 命令数の制限に到達
有料アップグレード（¥980 / 約50命令）
  ↓
継続課金
```

**既存200人は day-1 の転換源**。新チャンネル開始時に「講座の延長として、AIでデザインが作れるプラグインを作った」と案内するのが最強の導入。

## 目標（初期ドラフト）

| 期限 | 指標 | 数値 |
|---|---|---|
| 3 ヶ月後（2026-07） | 合算フォロワー（既存200 + 新規） | 800 |
| 3 ヶ月後 | プラグインインストール累計 | 300 |
| 3 ヶ月後 | 有料契約数 | 15（既存200人の7.5%転換想定） |
| 6 ヶ月後（2026-10） | インストール累計 | 1,500 |
| 6 ヶ月後 | 有料契約数 | 60 |
| 1 年後（2027-04） | 有料契約数 | 200 |
| 1 年後 | MRR | ¥200,000（¥980 × 200想定） |

## KPI ダッシュボード

- プラグインインストール数（Figma 管理画面）
- 無料 → 有料転換率
- 有料継続率（解約率）
- MRR
- SNS → プラグインページ流入数（UTM 推奨）
- 既存 YouTube 登録者からの流入割合
- **Method 別 views 平均 + プラグイン CV 数**（meta PDCA用。YouTube / Instagram を別々に）
- IG Reels の保存数・シェア数・リーチ（`performance-history.json` の `instagram.saved` / `shares` / `reach`）

### performance-history.json の指標

| キー | 取得元 | 更新 |
|---|---|---|
| `stats.views` / `likes` / `comments` | YouTube Data API v3 | `fetch-stats.mjs`（直近 14 日） |
| `instagram.mediaId` | 投稿時に `upload-instagram.mjs` → `record-upload.mjs` が記録。無い回は `fetch-stats.mjs` が IG 投稿一覧と JST 投稿日で突き合わせて復元 | 投稿時 / 初回取得時 |
| `instagram.views` / `reach` / `likes` / `comments` / `shares` / `saved` | Instagram Graph API `/{ig-media-id}/insights`（`plays` は廃止済みのため `views` を使用） | `fetch-stats.mjs`（直近 14 日 + 未取得の回） |

手動で更新だけしたいときは `gh workflow run fetch-stats.yml`（動画生成・投稿はしない）。

## データスキーマ拡張（enriched-*.json）

```json
{
  "date": "2026-04-21",
  "discovery": {
    "method": "figma-community",
    "description": "Figma Community の新着プラグインから AI 系を抽出",
    "sources": ["https://www.figma.com/community/plugin/..."],
    "query": "AI design generator new",
    "freshness_hours": 20
  },
  "articles": [ ... ]
}
```

## TBD（ユーザー側で決まり次第反映）

- [ ] プラグイン Figma Community URL
- [ ] YouTube チャンネル URL / 名前（既存デザイン講座チャンネル）
- [ ] Instagram アカウント名
- [ ] プランの詳細（¥980/50命令以外の 2 プランの価格と命令数）
- [ ] 無料枠の命令数
- [ ] トライアル期間の有無
