# ジャンル実験（日次 PDCA の上位層）— figma-navi-video の 2 アカウント

> **正本**: sns-hub [`docs/strategy/genre-experiment.md`](https://github.com/nannantown/sns-hub/blob/main/docs/strategy/genre-experiment.md)（2026-09-14 オーナー決定「AI系 IG 以外の 5 アカウントはジャンル/型を変えながら試す。コーヒーは販促維持」）。このファイルは、このリポの 2 アカウント分の**写し**。朝ルーチンは sns-hub を読めないので、**ジャンル実験についてはこの写しに従う**（正本との食い違いを見つけて直すのは横断 PDCA と人間の仕事）。
> 日々の「どのネタ・どの `discovery.method` が効くか」の最適化（`docs/strategy.md` の Meta PDCA）より、この写しの「**このジャンル/型そのものを続けるか切るか**」を 14 日ごとに判定する層を**優先する**。
> 戦略（ジャンル・型の中身）は `docs/strategy.md`、朝ルーチンの指示文は `docs/routine-prompt.md`（どちらもジャンル試行 #1 のカードが管理）。このファイルは両者と衝突しないよう別ファイルにしている。指示文や `scripts/pdca-summary.mjs` が数字を出す場合も、節の列・モード・判定日はこの写しに合わせる。
> この写しの書き換えは人間だけ（ルーチンは提案まで）。IG と YT は**別アカウント**として別々に評価し、数字を合算しない。

## このリポの試行台帳（写し）

| アカウント | 試行 # | ジャンル / 型 | 開始日 S | 型の初回投稿日 F | 導入時（2026-09-14）の判定 |
|---|---|---|---|---|---|
| IG Figmaナビ（既存デザインアカウント。ハンドル未記録） | #0 | 平日: デザインニュース / 週末: Tips・Before/After・UI 批評 / タイトル固定「【デザインニュース】…｜日付 #Shorts」 | 2026-09-14 | 2026-04-22 | 配信死亡（09-01..09-14: views 中央値 5 / 保存合計 0, n=12） |
| YT HAL- AI情報カフェ (@hal-ai-9000) | #0 | 同上 | 2026-09-14 | 2026-04-22 | 配信死亡（09-01..09-14: views 中央値 1, n=12） |

- 試行 #0 = 2026-04-22 の運用開始からの現行型。S はルール導入日（判定サイクルの起点）、F は現行型を始めた日。#0 の「経過日」は判定サイクル上の日数で、型そのものは F から続いている
- 台帳は 1 アカウント 1 行。新しい型を始めたら、**新しい型の初回投稿日の当日以降に**、sns-hub の台帳とこの表を同じ日に書き換える。同じ動画を IG と YT に出しているので、動画そのものを変えたら 2 行とも試行 # を進め、S = F = 新しい型の初回投稿日にする。例外: 型を変える PR が main に入り、次の定時投稿から新しい型になると決まっているときは、初回投稿日の当日なら投稿より前でも予定日で書いてよい（前日以前には書かない）。初回投稿が失敗・延期したら、新しい型が `data/performance-history.json` に最初に記録された日を S = F と読み替える（表は人間が直す）
- 試行 #1 の候補（別カードで準備中・未着地）: 新作 AI ツール TOP5（出典 Product Hunt）。**#1 の行は初回投稿日が確定したら更新する**（S = F = 初回投稿日、最初の判定日 = S + 14。main に入る前に予定日を書かない）。それまでは #0 のまま

## 判定窓・経過日・次の判定日と集計コマンド

d = 今日 − S（日数。日付はすべて JST）

| 条件 | 経過日 | 判定窓 | 次の判定日 |
|---|---|---|---|
| d < 0 | 未開始（S が未来の日付 = 台帳の書き間違い。前回モードと method 方針をそのまま続け、レポートの「注意」行に書く） | — | — |
| d ≥ 14 かつ d が 14 の倍数 | **判定日** | (今日 − 14)..(今日 − 1) | 今日 + 14 |
| それ以外 | Day (d mod 14 + 1) / 14 | max(F, 今日 − 13)..今日 | S + 14 × (floor(d / 14) + 1) |

例（S = 2026-09-14）: 09-15 は Day 2 / 14・判定窓 09-02..09-15・次の判定日 09-28。09-28 は判定日（判定窓 09-14..09-27）。09-29 は Day 2 / 14（次の判定日 10-12）。

リポのルートで次を実行すると、今日の日付から判定窓・経過日・次の判定日・判定指標がまとめて出る（S と F は上の台帳の値。日付を手で書き換えない）。

```bash
TODAY=$(TZ=Asia/Tokyo date +%Y-%m-%d); S=2026-09-14; F=2026-04-22
jq -r --arg today "$TODAY" --arg s "$S" --arg f "$F" '
  def day: strptime("%Y-%m-%d") | mktime / 86400 | floor;
  def ymd: . * 86400 | strftime("%Y-%m-%d");
  def median: sort | if length==0 then null elif length%2==1 then .[(length/2|floor)] else ((.[length/2-1]+.[length/2])/2) end;
  ($today|day) as $t | ($s|day) as $sd | ($f|day) as $fd | ($t - $sd) as $d
  | if $d < 0 then "today=\($today) status=未開始"
    else
      (if ($d >= 14 and ($d % 14) == 0) then {status:"判定日", from:($t-14|ymd), to:($t-1|ymd), next:($t+14|ymd)}
       else {status:"Day \(($d % 14)+1) / 14", from:([$fd, $t-13]|max|ymd), to:$today, next:($sd + 14*((($d/14)|floor)+1)|ymd)} end) as $w
      | [.videos[] | select(.date >= $w.from and .date <= $w.to)] as $v
      | ($v | map(select(.stats.updatedAt != null) | (.stats.views // 0))) as $yt
      | ($v | map(select(.instagram != null and .instagram.views != null) | .instagram)) as $ig
      | "today=\($today) status=\($w.status) window=\($w.from)..\($w.to) next_judgment=\($w.next) posts=\($v|length)",
        "YT views median=\($yt|median) (n=\($yt|length))",
        "IG views median=\($ig|map(.views)|median) saved_sum=\($ig|map(.saved // 0)|add // 0) (n=\($ig|length))"
    end
' data/performance-history.json
```

- 過去の判定日 J の判定（遅延判定）を出すときは、`TODAY` に J を入れて実行する（status=判定日、判定窓 (J − 14)..(J − 1) になる）
- jq の日付関数が使えない環境では、上の表どおりに `date -d` で判定窓を出し、同じ条件（YT は `stats.updatedAt` あり、IG は `instagram.views` あり）で中央値と合計を出す
- 2026-09-16 を TODAY にした検算: Day 3 / 14、判定窓 09-03..09-16、次の判定日 09-28、YT views 中央値 0.5 (n=10)、IG views 中央値 5 / 保存合計 0 (n=10)

## 判定指標

- **IG**: 判定窓の `instagram.views` の**中央値**と、`instagram.saved` の**合計**（別々の 2 指標で、足さない）。`instagram` か `instagram.views` が null の回は除外
- **YT**: 判定窓の `stats.views` の**中央値**。`stats.updatedAt` が null の回は除外
- n は IG / YT で別々に数える。**n < 7 なら判定保留**。IG と YT の数字を足したり平均したりしない
- 朝ルーチンの時点では当日分は未投稿で、前日分も統計・insights が未取得のことが多い（除外されるので、通常日の n は最大 12 前後）

## 閾値（表の上から順に当てはめ、最初に該当した行が判定値。初期値・仮置き）

| 判定 | IG | YT |
|---|---|---|
| **配信死亡** | views 中央値 < 10 **かつ** 保存合計 < 5 | views 中央値 < 5 |
| **切替候補** | views 中央値 < 50 **かつ** 保存合計 < 5 | views 中央値 < 20 |
| **続行** | 上記以外 | 上記以外 |

- 判定は指標だけで機械的に出す（「もう少し様子を見たい」は理由にしない）。閾値はルーチンが変えない（おかしいと思ったら「戦略更新提案」に書く）
- 個別の実験メモが独自の評価を持っていても、判定値とモードはこの表の閾値で判定日に出す（実験メモの評価は「ジャンル判定」節の根拠として引用してよい）
- 同じ試行で判定日の「続行」が 3 回（42 日）続いたら、その判定日に「伸びしろが残っているか」を一言添える

## モードの決め方（毎朝、アカウントごと）

モードは `通常` / `切替候補` / `配信死亡モード` の 3 つ。**モードが変わるのは判定日（と遅延判定）だけ**で、それ以外の日は前回モードを続ける（行ったり来たりを防ぐ）。

1. **前回モード** = `docs/pdca/` の `YYYY-MM-DD.md` のうち、**今日より前の日付で「ジャンル試行の状態」表のモード列が読める最新のもの**のモード列。表の無いレポート（ルーチンの失敗など）は飛ばして、さらに前の日付を見る。1 本も無いときだけ、上の台帳の「導入時の判定」（配信死亡 → `配信死亡モード（2026-09-14〜）`）。**新しい試行が始まっても前回モードを引き継ぐ**（新しい試行の最初の判定日に、その試行の数字で判定し直す）
2. **遅延判定**: 今日より前の直近の判定日 J について、J 以降の日付のレポートに「ジャンル判定」節が無ければ（ルーチンの失敗・導入の遅れなど）、今日まず J の判定を窓 (J − 14)..(J − 1) で出し、3. の判定日と同じ扱いでモードを決め直してから今日の分に進む。判定日は S + 14k（k ≥ 1）だけで、S 当日（2026-09-14 の「導入時の判定」）は判定日ではないので、遅延判定の対象にしない
3. **決定**:
   - **今日が判定日（または遅延判定）** → 判定値どおり: 配信死亡 → `配信死亡モード`（前回から続くなら宣言日はそのまま、新しく入るなら宣言日 = その判定日）/ 切替候補 → `切替候補` / 続行 → `通常` / 判定保留（n < 7）→ 前回モードのまま
   - **それ以外の日** → 前回モードのまま。今日の判定値が配信死亡なのにモードが `配信死亡モード` でないときは、レポートに「注意」行を書く（モードの変更は次の判定日）

## 通常・切替候補のときの日次 PDCA（Meta PDCA の補足）

- method の比較・80/20 は、**型の初回投稿日 F 以降の回だけ**で行う（新しい型の序盤に古い型の数字を混ぜない）。method 分析に使う「直近 14 日」は判定窓とは別で、**今日より前の 14 日**（今日 − 14..今日 − 1）
- method の順位は**アカウントごとに**付ける（IG の TOP 3 と YT の TOP 3 を別々に出し、合算スコアを作らない）。80/20 の exploit 候補は、配信死亡モードでないアカウントの TOP 3 から選ぶ
- 切替候補のアカウントは、判定日に次ジャンル候補を 2〜3 案出す（このリポの制約を守る）。日々の method 最適化は通常どおり続ける

## 配信死亡モード中の日次 PDCA

1. **method 切り口の最適化を止める**: 配信死亡モードのアカウントの数字で method の TOP/WORST 比較・Method 提案・80/20 選定・WORST method の凍結をしない（0 と 1 を比べても何も分からない）
   - **2 アカウントとも配信死亡モード** → method は**性能データで選ばない**。曜日の型・「直近 2 日と同じ method を連続で選ばない」・NG パターンを満たす method のうち、**直近 14 日の使用回数（`data/performance-history.json` の、今日より前の直近 14 日の `discovery.method` で数える）が少ないもの**を選ぶ（同数なら `docs/strategy.md` の method 表の上から）。WORST method の凍結はすべて解除して扱う。レポートの「Method 提案」節は「配信死亡モードのため省略」と 1 行だけ書く。method 別の表を出す場合は参考表示のみ
   - **片方だけ配信死亡モード** → method の比較と 80/20 は**生きている側のアカウントの指標だけ**で行う。生きている側も判定窓の n < 7 で指標が使えない日は、2 アカウントとも配信死亡モードのときと同じ選び方をする
2. **提案は構造実験だけ**: タイトル個別化 / 型変更 / ジャンル変更。各提案に「何を変えるか / 何で測るか / 14 日後の合格ライン」を書く。合格ラインの既定は上の閾値表の「続行」域（IG: views 中央値 ≥ 50 または保存合計 ≥ 5 / YT: views 中央値 ≥ 20）で、配信死亡を抜けるだけの線を合格にしない。動画そのものを変える提案（型変更・ジャンル変更）は `IG / YT 共通` の 1 行にまとめてよい。片方だけの変更（タイトル個別化など）はアカウント別に書く
3. **提案はレポートに書くだけ**: 当日の原稿・タイトル・`data/enriched-*.json` には反映しない（型・スキーマ・必須フィールドは従来どおり。反映すると動画生成が失敗しうる）。次ジャンル候補や構造実験は手持ちのデータと知識で短く書き、Web 調査に時間を使わない
4. **新しい試行が走っている間**（試行 # が #1 以降で、まだ最初の判定日前）は、「構造実験の提案」節に `実行中: 試行 #n（<ジャンル / 型>）Day N / 14` と書き、追加の提案は判定日まで控える（変数を増やさない）。台帳の注記で別カードが準備中の試行があり、まだ初回投稿が無い間も同じで、`準備中: <ジャンル / 型>（別カードで実装中）` とだけ書き、同じ案を毎朝書き直したり別案を足したりしない
5. **このリポの制約**: ジャンル・型・タイトル方針のすべてを変えてよい
6. **投稿は止めない**。ネタ調査・原稿生成は通常どおり。型の切替は人間が決めて PR で入れる

## 日次レポート（`docs/pdca/YYYY-MM-DD.md`）の冒頭に書く節

タイトル直下に次の節を書く。判定日（と遅延判定）なら「ジャンル判定」、配信死亡モードのアカウントがある日は「構造実験の提案」も書く。判定指標の現在値は IG と YT で別の書き方をし、足さない。

```markdown
## ジャンル試行の状態

| アカウント | 試行 # | 開始日 | 経過日 | 判定窓 (n) | 判定指標の現在値 | モード | 次の判定日 |
|---|---|---|---|---|---|---|---|
| IG Figmaナビ | #0 | 2026-09-14 | Day N / 14（判定日なら「判定日」） | MM-DD..MM-DD (n=…) | views 中央値 … / 保存合計 … | 通常 / 切替候補 / 配信死亡モード（YYYY-MM-DD〜） | YYYY-MM-DD |
| YT HAL- AI情報カフェ | #0 | 2026-09-14 | … | MM-DD..MM-DD (n=…) | views 中央値 … | … | YYYY-MM-DD |

- 今日の判定: なし（判定日ではない） / 判定日 →「ジャンル判定」節 / 遅延判定を行った場合はその旨
- 注意（該当時のみ）: <アカウント> は判定窓で配信死亡の域（モードの変更は次の判定日）
- 今日の method 方針: 通常（Meta PDCA・80/20。IG と YT は別々に順位づけ） / 性能データで選ばない（2 アカウントとも配信死亡モード） / <生きている側のアカウント> の指標だけで選ぶ

## ジャンル判定（判定日・遅延判定のときだけ）
- <アカウント>: 続行 / 切替候補 / 配信死亡 — 根拠（指標値と閾値） — モードの変化 — 次ジャンル候補 2〜3 案（切替候補・配信死亡のとき）

## 構造実験の提案（配信死亡モードのアカウントがある日のみ）
- IG / YT 共通 または <アカウント>: <タイトル個別化 / 型変更 / ジャンル変更> — 何を変えるか / 何で測るか / 14 日後の合格ライン（試行 #1 以降の実行中は「実行中: 試行 #n（<ジャンル / 型>）Day N / 14」、準備中は「準備中: <ジャンル / 型>（別カードで実装中）」とだけ書く）
```
