# Jev の毎朝ニュース PDCA — 2026-09-25

## ジャンル試行 #2（Jev の毎朝ニュース）の状態 — 2026-09-25

- 開始日 2026-09-24 / 投稿 1 本 / 終了条件: オーナーの停止指示（判定日なし）
- IG views 中央値 —（n=0）/ IG 保存合計 0 / IG フォロワー増 +0（2026-09-24 0 → 2026-09-24 0）
- YT views 中央値 —（n=0。IG と合算しない）
- IG insights は最大 48 時間遅れる。前日・当日の値は暫定

| 種類 | 本数 | IG views 中央値 | IG 保存合計 | YT views 中央値 |
|---|---|---|---|---|
| intro | 1 | — | 0 | — |

| 日付 | 段階-回 | 種類 | 話題 | IG views | IG 保存 | YT views |
|---|---|---|---|---|---|---|
| 2026-09-24 | 1-1 | intro | intro-what-is-jev | 未取得 | — | 未取得 |

## 気づき

- 初回投稿（2026-09-24 1-1 `intro-what-is-jev`）は IG も YT もまだ数値取得なし。performance-history にも未反映で、insights は最大 48 時間遅れの想定内。PDCA としての傾向判断は最低でも n=3〜4 本たまるまで保留。
- `aired-check --self-test` は `NG — gh-missing: the gh CLI is not installed` を返した。この cloud env には `gh` が入っておらず、GitHub 操作は MCP ツール経由で行っている。従って手順 0 の「aired-check が投稿ログを読めるか」の確認は自動化できていない。今日は `next` の `redoCandidate` が `null`（最新の回に投稿記録抜けなし）なので `aired-check` の本判定は不要だが、記録抜けが発生した日は self-test 落ちのままだと `aired-check` が動かない可能性が高い（オーナーの手動介入か、gh を CI/env に入れる判断が必要）。
- X の `data/jev-x-posts.json` は `fetchedAt: 2026-09-24T21:36Z`（JST 06:37）で今朝更新済み。`errors` には想定どおり `community search: …` のみ（`@typesafeai` / `@CompleteSkeptic` の失敗行はない）。第1段階の題材には X を使わないが、以後の使用例回に向けて公式・CEO の投稿は蓄積されている。

## 今日の Action（3 つまで）

1. 第1段階の順番固定に従い `intro-no-text`（1-2）を書く。オーナー指定の「必ず入れること = 選択肢・数値＋確信度／使うのは人ではなくソフト」を4スライドで明示。
2. 数字（0〜1 の確信度、0.94、0.5）はすべて出典を明記し、会社主張は「Spring AI の解説では」で名指す。倍率や「ハルシネーションしない」は本回では扱わない（1-4/1-6 の題材）。
3. 出典は当日 WebFetch できたページ（`github.com/typesafe-ai/typesafe-sdk-python`、`spring.io/blog/2026/09/21/spring-ai-typesafe-structured-judgment/`）＋オーナー確認済みの公式ブログのみに絞る。docs.typesafe.ai は egress で開けなかったため `sources` に載せない。

## 今日の回（段階-回 / 種類 / topic_key / 選んだ理由 / 見送った候補と理由 / 見たページの数）

- 段階-回: 1-2
- 種類: intro
- topic_key: intro-no-text
- 選んだ理由: `scripts/jev.mjs next` が第1段階の 2 回目として `intro-no-text` を指定。テーマは「文章を書かない AI — 答えを『型』と確信度で返す」で、必ず入れることは「選択肢・数値＋確信度、使うのは人ではなくソフト」。順番固定なのでこれ以外の選択肢はない。
- 見送った候補と理由:
  - 1-3〜1-6（LLM との違い／速度／料金／ハルシネーション）: 順番固定でまだ 1-3 に進めない。
  - 使用例（LiteLLM のルーター、Spring AI の実装コード）: 第2段階の題材。今日入れると第1段階の「Jev の中身を先に説明する」設計が崩れる。
  - CEO のツイート（20〜200 倍の言及）: 速度の話は 1-4 で扱う予定なので今日は避けた。
- 見たページの数: `research.checked` に 2 件（typesafe-sdk-python README、spring.io の解説）。WebFetch は typesafe.ai / docs.typesafe.ai / marktechpost / analyticsvidhya / kingy.ai / fly.io / daily.dev / dev.to / cloudflare / mindstudio / easternherald / flaviocopes すべて egress proxy でブロックされ、代わりに WebSearch の要約で骨子を突き合わせた（同じ Choice/Score/confidence の仕様が複数の第三者記事で一致していることは確認）。

## 戦略更新提案（任意）

- **cloud env に `gh` が無い件**: `aired-check` は `execFileSync("gh", …)` を前提にしているため、gh 無しでは記録抜けの日にやり直し判定ができない。案 A: env 側に gh を入れる（他のシェル系ワークフローとの依存を増やす）。案 B: `aired-check` を GitHub MCP 経由で叩ける代替パスを持たせ、`--self-test` がどちらでも通るようにする（jev.mjs の書き換えが要る）。案 C: 記録抜けが起きた日はオーナーが Actions のログを見て手動でやり直すルールに落とす（今のところ発生していないので、当面 C で運用しつつ、次に発生した日にオーナー相談で A / B を選ぶ）。今日は本ルーチンでは動かさず、レポートに書くのみ。
- **egress proxy が第三者記事を弾く件**: 今日は typesafe.ai / docs.typesafe.ai / MarkTechPost / Analytics Vidhya / Cloudflare / MindStudio / TechCrunch 系がすべてブロック。第1段階の間は WebSearch 要約＋オーナー確認済みの docs/jev-format.md でしのげるが、第2段階（使用例）や第3段階（毎朝の新情報）では第三者記事の一次確認が要る。運用が本番化する前にオーナーに proxy allowlist の拡張可否を相談したい（本回は提案の記録のみ、リクエストは出していない）。
