# Jev の毎朝ニュース PDCA — 2026-09-24

## ジャンル試行 #2（Jev の毎朝ニュース）の状態 — 2026-09-24

- まだ投稿がありません（初回投稿の日が開始日になります）。終了条件はオーナーの停止指示で、14 日ごとの判定はありません。

## 気づき

- 今日はシリーズ初回。`data/jev-episodes.json` の `episodes` が空、`node scripts/jev.mjs next` は第1段階 1 回目・`intro-what-is-jev`（テーマ「Jev って何？ 誰が作った、どんな AI か」）を指示。この env では `data/content-format.json` が `"jev"` に切り替わった直後の朝ルーチンで、開始日は本日となる予定。
- **aired-check の self-test は NG**（`gh-missing: the gh CLI is not installed`）。この cloud env には gh CLI が入っておらず、投稿ワークフローのログ読み直しに使う黙りの再投稿防止機構は今日は働かない状態。今日は初回でやり直しの余地がないので実害はないが、後日 `redoCandidate` が出た日は aired-check が黙って `unknown` で終わる可能性が高い。オーナーに報告して、cloud env に gh を入れるか、mcp__github__ 経由で actions ログを読む代替経路を仕込むか検討が要る。
- **primary source が egress proxy に弾かれる**: typesafe.ai / docs.typesafe.ai / heise / yahoo / techcrunch / latent.space / wikipedia / tomshardware / mindstudio / datacamp / getmaxim / truefoundry / etvbharat / easternherald 等がすべて `EGRESS_BLOCKED` になった。github.com は通ったので TypeSafe AI 組織ページと typesafe-sdk-python の README を開いて会社の実在と `system_one()` メソッドの型付き出力を直接確認。WebSearch の要約（8 件以上の独立記事にわたって一致）で公開日・CEO・InstructGPT 共著・System One の骨子を突き合わせた。今日は 1-1 のような「一次情報がすでにオーナーの format doc で確認済みの基礎事実」の回だったので投稿を止めずに進めた。**同じ状況が続くと、より新しい話題を扱う第3段階でつらくなる**（primary の記事本文を開けないと確度が担保できない）。ネットワーク設定にオーナー確認が必要。

## 今日の Action（3 つまで）

1. 第1段階 1-1 を「TypeSafe AI・9/15 公開・CEO は InstructGPT 共著者」の 3 点を軸に、4 スライド構成で公開する（オーナー指定の必須項目を全て埋める）。
2. 会社の主張（40〜200 倍）はスライドに置き `claim_source: "TypeSafe の発表"` を付け、ナレーションでも「TypeSafe によると」と誰の話かを名指し、第三者（LiteLLM）の実測を「数倍」と併記する。
3. 一次情報が読めなかった経緯を `research.note` に残し、`research.checked` は今日実際に開けた GitHub のページだけを記載する（作り話をしない・見ていないものは見たと書かない）。

## 今日の回（段階-回 / 種類 / topic_key / 選んだ理由 / 見送った候補と理由 / 見たページの数）

- 段階-回: **1-1**
- 種類: **intro**
- topic_key: **intro-what-is-jev**
- 選んだ理由: `node scripts/jev.mjs next` の指示（第1段階 6 テーマは順番固定）。シリーズ初回なので選ぶ余地はない。オーナーの「最初は誰も Jev を知らないので、いきなりニュースを流さない」方針にそのまま従う。
- 見送った候補と理由: 今日は 1-1 だけが正しい選択肢。primary source が読めない状況で「解説回（`kind: "explainer"`、`topic_key` は `intro-` で始めない）」に落とすかも検討したが、必須の 3 事実（9/15 公開・TypeSafe AI・CEO が InstructGPT 共著）は WebSearch と GitHub 直読みで一致確認できたため、intro のまま出した。
- 見たページの数: 直接 WebFetch できたのは 2 ページ（github.com/typesafe-ai と github.com/typesafe-ai/typesafe-sdk-python）。加えて WebSearch を 2 回（launch/CEO 関連、InstructGPT 共著）実行し 15 件超のリンクを要約経由で確認。primary source は 12 件以上 attempt したがすべて egress proxy に拒否された。

## 戦略更新提案（任意）

- **cloud env に gh CLI を入れるか、`mcp__github__actions_get` / `actions_list` / `get_job_logs` を使って aired-check を書き直すか**、オーナー判断が要る。今のままだと、`redoCandidate` が出た日に「投稿の記録が抜けたが実際は投稿されていた」ケースを検出できず、`unknown` で普段の枠に流れる（二重投稿は防げるが、本当に投稿失敗していた日を静かに見逃す）。
- **egress proxy が primary source を弾く問題**を、この routine の運用に間に合う範囲でどうするか。案 A: WebFetch の許可リストに typesafe.ai / docs.typesafe.ai / 主要英語テック媒体（TechCrunch, The Register, Ars Technica, heise 等）を足す。案 B: 手続きが取れないなら、朝ルーチン側で「WebSearch の要約に一致確認が取れれば primary の直読みなしでも OK」を明文化。案 A の方が安全。第3段階の「毎朝の新情報」が始まる前（第2段階 = 使用例 5 回、第1段階 6 日と合わせて 10 月頭くらい）に片付けたい。
